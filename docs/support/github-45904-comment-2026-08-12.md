# GitHub #45904 Comment — 2026-08-12 — Project ebptjjsmeltykqqvcvqo

---

Seeing the same issue on project `ebptjjsmeltykqqvcvqo` (org `cecebefree`, Free plan). PostgREST stuck on `public` schema only — 47 paths, zero for custom schemas `school_desk`, `front_desk`, `office_desk`.

Management API config confirmed correct (`db_schema: "public,graphql_public,school_desk,front_desk,office_desk"`). Attempted:
- `supabase config push` ✅
- Management API PATCH ✅
- `ALTER ROLE authenticator SET pgrst.db_schemas` ✅
- `postgrest.pre_config()` function ✅
- 5× pause/restore cycles ✅
- Dashboard SQL Editor `NOTIFY pgrst, 'reload schema'` ✅

Still stuck. Verified 2026-08-12 06:45 UTC via curl — empty output for all three schemas.

Happy to share full reproduction steps or logs if useful. This is blocking a multi-schema migration (Row 55 of our implementation plan).
