{
  "name": "redhouse",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo run dev --filter=!@redhouse/mobile --filter=!@redhouse/cli",
    "build": "turbo run build",
    "lint": "biome lint .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint:types": "bash scripts/check-database-types.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.10.1",
    "typescript": "^5.7.2"
  }
}
